<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 配置批量扫描的别名的方式 -->
	<typeAliases>
		<package name="com.kekemile.po" />

	</typeAliases>

	<!-- 配置Mapper 由于我们使用spring 跟 maybatis 的扫描包 不需要这些 mapper的配置 必须遵循的规则 配置别名 -->


</configuration>
